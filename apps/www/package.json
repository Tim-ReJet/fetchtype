{
  "name": "@fetchtype/www",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp styles.css dist/styles.css && cp script.js dist/script.js",
    "dev": "python3 -m http.server 4173",
    "preview": "python3 -m http.server 4173 -d dist"
  }
}
